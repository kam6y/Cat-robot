# Supertonic通常アプリ統合の実機検証

2026-09-26、`feature/supertonic-voice-integration`。main起点は`7e8c9b0`、全10声と修正前比較のビルドは`472aa3e4500165ed668a0cd1bd3a2bbb0664f3d2`。レビュー修正後の採用評価は`c6fc488`のビルドを使う。

## 実装

- 通常アプリの音声はSupertonic 3。F1を初期値とし、F1〜F5/M1〜M5を選択・保存する。
- 設定を開く際は会話を停止し、閉じても自動再開しない。会話記憶は変更しない。
- iPhone/Bluetoothの読み補正は合成入力だけに適用。字幕と記憶は原文。URL・メール・コード・識別子内は保護する。
- 文ごとの直列合成と一文先読みを実装。先読みは再生中の一文に加え次の一文だけを保持し、ネイティブ推論を同時実行しない。停止時は推論終了を待ち、古いPCMを破棄する。
- 性能条件と利用者の聴感確認を満たし、製品の再生方式にsentencePrefetchを採用した。Appleの実装は保全し、自動フォールバックはしない。

## 測定条件

iPhone 16 Pro (`iPhone17,1`)、iOS 27.2、USB接続。固定のSupertonic 3 / ONNX Runtime 1.24.2、日本語、F1、CPU 2 threads、8 steps、speed 1.05。全10声試験だけ声を変更する。Gemmaは通常と同じGPU/12K/返答160トークン、試験専用の空メモリを使用し、利用者の記憶を変更しない。

時刻は単調時計とPCM再生コールバックによる。開始はrequestから最初のspeechStarted、文間は前のspeechFinishedから次のspeechStarted、合計はrequestからfinished。外部録音による音響計測ではなく、モデルが音声内に含める無音は文間値に含まれない。firstSentenceの後半は複数文をまとめて合成するため、その内部文間も計測対象外。

fixedは同じ6文章×3方式×3反復=54試行、反復ごとに方式順を回転する。Gemmaは6プロンプト×2方式×3反復=36試行で生成文差があるためfixedと混ぜない。serious/criticalのときは試行前に冷却し、その待機も記録する。p95はnearest-rank。

## 完了した検証

- 10声の実合成・再生: 10/10成功。開始中央値0.518秒、p95 0.609秒。発熱serious/critical到達なし。F1以外の品質順位を再評価する試験ではない。
- 通常Swift試験: 358件、実機専用8件skip、失敗0（350件実行）。
- Python試験: 26/26成功。Rubyプロジェクト生成契約: 成功。
- 署名付き実機build-for-testing: 成功。

生データは[data](data/2026-09-26-supertonic-voice-integration/)へ保存。runIDとソースrevisionを含め、別実行の結果が混ざっていないことを集計時に検査する。ローカルのビルドログ・xcresultは`/private/tmp/cat-supertonic-integration/`にある。

## 実装中の判断

- 検証済み比較ブランチの音声エンジンは書き直さず移植した。新しい構成のテストで接続を確認したが、再実装による独立検証ではない。
- 既存ライフサイクル試験のTask.yieldによる順序依存を修正した。後から届く非アクティブ通知より前に完了した権限再確認は許容し、実際のマイク・音声再開回数とpaused状態は厳密なまま維持する。余分な権限確認自体を見逃す可能性は残る。

## 固定文の比較（レビュー修正版）

54/54試行成功。合成入力原文の連結と返答全文の不一致0、各合成入力に対する開始・終了イベントの欠落/重複0。全方式でserious/critical到達0、冷却待機0。

| 方式 | 開始中央値 / p95 | 文間中央値 / p95 | 合計中央値 / p95 | ピークメモリp95 |
|---|---:|---:|---:|---:|
| 最初の一文＋残りまとめ | 0.795 / 1.324秒 | 1.097 / 2.216秒 | 8.517 / 20.057秒 | 623 MB |
| 一文ずつ直列 | 0.791 / 1.328秒 | 0.666 / 1.306秒 | 10.373 / 19.772秒 | 622 MB |
| 一文先読み | 0.800 / 1.332秒 | 0.076 / 0.086秒 | 8.965 / 17.678秒 | 612 MB |

一文先読みの開始p95悪化は約7msで、100ms以内の条件を満たす。文間中央値・p95も改善。一文ごとの合成は話し方自体を変えるため、全体時間が常に短くなるわけではない。fixture別の合計中央値は以下。

| 文章 | 最初の一文＋残りまとめ | 一文先読み |
|---|---:|---:|
| 二文の挨拶 | 4.656秒 | 4.192秒 |
| 短い五文 | 6.678秒 | 8.240秒 |
| 長い三文 | 20.033秒 | 17.666秒 |
| 引用・数字 | 10.335秒 | 9.688秒 |
| 英単語 | 10.805秒 | 9.755秒 |
| 句点なし | 5.708秒 | 5.700秒 |

短い五文では全体が約1.56秒長くなった。文の境界で合成される無音や抑揚の違いがあり、文間コールバック値だけでは聴感を判断しない。固定文の測定はCPUの音声合成のみで、Gemma同時利用の結果は別に評価する。

## Gemma併用と停止確認

Gemma併用は36/36成功。生成内容は方式間で同一ではないため、開始時間の差を音声方式だけの効果と解釈しない。合成入力原文の連結不一致0、各文の開始・終了ordinalは期待した順序と一致し欠落/重複0。高温到達0、冷却待機0、メモリ不足終了0。

| 方式 | 開始中央値 / p95 | 文間中央値 / p95 | 合計中央値 / p95 | ピークメモリp95 |
|---|---:|---:|---:|---:|
| 最初の一文＋残りまとめ | 1.747 / 3.356秒 | 1.166 / 3.346秒 | 12.854 / 23.010秒 | 1.763 GB |
| 一文先読み | 1.345 / 3.274秒 | 0.076 / 0.096秒 | 12.147 / 21.638秒 | 1.760 GB |

実機でのライフサイクル5件（生成中の停止、再生中の停止、background、音声割り込み、route change）はすべてpausedへ移行し、その後の実合成・再生も成功。イベントはテストから注入しており、ホーム操作・電話着信・イヤホン抜去そのものを試した結果ではない。停止中の先読み推論終了待ちと古いPCMの破棄は、別途ゲートで制御した単体試験でも検証している。

## オフライン再生（2026-09-26の再試聴）

利用者が機内モード・Wi-Fiオフ・Bluetoothオフと画面復帰を確認した後、モデル読み込み前の待機を解除した。`6631865D-FEA4-4D3D-B330-ED3929B4A220`でGemma生成とF1合成・再生は2/2成功。2方式とも同じ三文が生成され、合成入力原文の連結も返答と一致した。

| 方式 | 開始 | 文間 | 合計 |
|---|---:|---:|---:|
| 最初の一文＋残りまとめ | 2.897秒 | 1.400秒（1境界） | 14.592秒 |
| 一文先読み | 1.872秒 | 0.140秒（2境界の中央値） | 12.809秒 |

各方式1試行の動作確認であり、性能の分布推定には使用しない。高温到達・冷却待機・メモリ不足終了なし。通信OFFは利用者の設定確認によるもので、パケット監視ではない。

先行した待機試行`E0F704F5`は確認待ち600秒でskip、`927A72DC`はオフライン再起動時のiOS開発用証明書確認でテスト開始前に失敗した。どちらも合成成功件数に含めない。Wi-Fiを一時的にオンにして起動し、モデルロード前で再び通信を切る手順で最終確認した。製品の合成処理がネットワークを必要とすることを示す失敗ではない。

## 独立レビュー

新しい文脈の独立レビューでCritical 0 / Important 2 / Minor 0。以下の2件を、修正前に失敗する回帰テストと修正後の全体試験で確認した（`68851f7`）。

1. 文キューが満杯の間も生成失敗を検知できるよう、上流の読み取りを送信待ちから分離。累積snapshotは最新1件だけを保持し、全文2,000文字を上限とする。置き換わるsnapshotに過去の本文が含まれるため、文そのものは落とさない。8文の欠落・重複と失敗時の即時停止を検証した。
2. 大文字・大小混在HTTPスキームも文分割で保護し、URLのクエリが分割後に読み補正される不具合を修正した。

コード完成後、長時間の実機比較と独立レビューを並行させた。最終の方式選択は主担当が実機結果と利用者の再試聴に基づいて行った。レビュー後の変更を独立レビューが見ていないという限界はあるが、修正2件の失敗再現・全体試験、修正版の実機再測定、最後の標準設定の構成試験で確認した。レビュー後の修正は回帰テストと全体試験で確認し、修正版でも実機測定を行う。

## 通常画面での最終確認

2026-09-26、利用者が次の両方を「両方問題なし」と確認した。F2を選択し、アプリ終了・再起動後もF2が保持されることを確認してF1へ戻した。USBを外して会話を開始し、「猫ちゃん、こんにちは」に対して音声で返答した。

## 現時点の採用状態

F1を初期値とする全10声選択・保存・読み補正は通常アプリへ反映済み。一文先読みは固定文で文間・開始p95・メモリ・熱の条件を満たし、Gemma併用と停止試験も成功した。2026-09-26のオフライン再試聴で利用者が「違和感なし。これでよい」と確認したため、製品の既定にsentencePrefetchを採用した。通信OFFの再生は成功。通常画面の選択保存・再起動・USBなし会話も利用者が確認済み。mainには未マージ。

通常版の採用コミットは`d40b728`。標準構成の変更前に構成テストが失敗することを確認し、変更後の全Swift試験は358件/実機専用8skip/失敗0。プロジェクト再生成後の差分なし、生成契約成功。署名付き通常版ビルド・実機への更新・通常起動まで成功した。

## マージ前のsimplify

`main 7e8c9b0...feature 6777760`の固定差分を、再利用・単純化・効率・抽象化の4担当で並行レビューした。各担当は読み取り専用とし、主担当が全指摘を確認して次の7件を反映した。

- 文の内容判定を既存`ReplySentenceBuffer.isContent`へ統一。
- テストの専用推論ゲートを既存`ConversationTestGate`へ置換。
- 読み補正の固定正規表現を静的に一度だけ構築。
- トークン検証を入れ子の`allSatisfy`にし、中間配列の生成を削除。
- 資産検証から検証済みmanifestを返し、直後の再読み込み・再decodeを削除。
- 声設定が閉じている条件を算出プロパティにまとめ、同じ条件の5重記述を削除。
- 通常アプリで使われていない旧`SupertonicSpeaker`をテスト側へ移動し、既存の基準試験を維持。

見送り: 音声スタイルのキャッシュ化はオブジェクトの保持期間とメモリ使用を変えるため、この動作を変えない整理には含めない。モデル・合成条件・先読み方式・停止処理の契約は変更していない。

整理後の既存関連テスト43件は成功（skipなし）。プロジェクト生成契約と再生成の決定性も成功。全体試験と実機測定の上記結果は整理前のもの。この整理では実機への再インストールや再測定は行っていない。
